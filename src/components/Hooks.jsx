import React, { Component, useState } from 'react';

function Hooks() {
    const [count, setCount] = useState(0);









    return <h1 className={"font-sans text-3xl font-bold"}>Hello hooks!</h1>;
}

export default Hooks;